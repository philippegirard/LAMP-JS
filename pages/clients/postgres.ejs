<%
// Get PostgreSQL version
result = await postgres.query('SELECT version()');
postgresVersion = result.rows[0].version;
%>

<h2>PostgreSQL Connected Successfully!</h2>
<p>Version: <%= postgresVersion %></p>